/** Shared organisation login — no user database. */

export const SESSION_COOKIE_NAME = "dba_session";

/** Session lifetime: 10 hours */
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 10;

export const DASHBOARD_HOME = "/in-behandeling";
